import headerLogo from "../../img/logo.svg";

const HeaderLogo = () => {
  return (
    <div className="header__logo">
      <img src={headerLogo} alt="logo delivery app" />
    </div>
  );
};

export default HeaderLogo;
